# PyTAAA Web Environment Configuration
#
# Copy this file to .env and fill in your actual values:
#   cp .env.example .env
#
# The .env file is loaded automatically by pydantic-settings.
# Never commit .env to version control!

# =============================================================================
# Database Configuration
# =============================================================================

# PostgreSQL connection settings
# For local development (outside Docker): use localhost
# For Docker deployment: use 'db' (the service name in docker-compose.yml)
POSTGRES_SERVER=localhost
POSTGRES_USER=pytaaa_user
POSTGRES_PASSWORD=pytaaa_pass
POSTGRES_DB=pytaaa_db

# Full database URL (optional - constructed from above if not set)
# Format: postgresql+asyncpg://USER:PASSWORD@HOST:PORT/DBNAME
# DATABASE_URL=postgresql+asyncpg://pytaaa_user:pytaaa_pass@localhost:5432/pytaaa_db

# =============================================================================
# Application Settings
# =============================================================================

# API version prefix
API_V1_STR=/api/v1

# Project name (used in logs and documentation)
PROJECT_NAME=pytaaa_web

# Enable SQL query logging (set to 'true' for development, 'false' for production)
# SQL_ECHO=false

# =============================================================================
# Data Paths
# =============================================================================

# Path to PyTAAA data directory containing model subdirectories
# Default for local development:
PYTAAA_DATA_DIR=/Users/donaldpg/pyTAAA_data

# For Raspberry Pi deployment, use:
# PYTAAA_DATA_DIR=/home/pi/pyTAAA_data

# =============================================================================
# Security (for production deployment)
# =============================================================================

# Secret key for session management (generate with: openssl rand -hex 32)
# SECRET_KEY=your-secret-key-here

# Allowed hosts (comma-separated)
# ALLOWED_HOSTS=localhost,127.0.0.1,yourpi.duckdns.org

# =============================================================================
# Deployment Notes
# =============================================================================
#
# Local Development:
#   1. Copy this file: cp .env.example .env
#   2. Adjust PYTAAA_DATA_DIR if your data is in a different location
#   3. Run: docker-compose up -d db
#   4. Run: alembic upgrade head
#   5. Run: ./start_pytaaa_web_db.sh
#
# Raspberry Pi Deployment:
#   1. Copy this file to the Pi: scp .env pi@raspberrypi:/home/pi/pytaaa_web/
#   2. On the Pi: edit .env and set POSTGRES_SERVER=db
#   3. On the Pi: set PYTAAA_DATA_DIR=/home/pi/pyTAAA_data
#   4. Run: docker-compose up -d
#
# See docs/RASPBERRY_PI_DEPLOYMENT.md for full deployment instructions.
